import React from 'react';

export const NewProject = () => (
  <>
    <div className="content-header">
      <div><h2>Subir Nuevo Proyecto</h2><div className="sub">Completa los datos para registrar un nuevo diseño.</div></div>
    </div>
    <div style={{ background: 'var(--panel)', padding: '24px', borderRadius: '12px', border: '1px solid var(--line)' }}>
      <p style={{ color: 'var(--text-muted)' }}>Formulario en construcción (se conectará con Django).</p>
    </div>
  </>
);